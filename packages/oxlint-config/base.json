{
  "$schema": "https://raw.githubusercontent.com/oxc-project/oxc/main/npm/oxlint/configuration_schema.json",
  "plugins": ["import", "typescript", "unicorn", "oxc"],
  "categories": {
    "correctness": "error",
    "perf": "warn",
    "suspicious": "warn",
    "restriction": "off",
    "style": "off",
    "pedantic": "off"
  },
  "rules": {
    "import/no-unassigned-import": "off",
    // The `style` category is off wholesale, which is why this one drifted to 42
    // violations. It is a stated project convention (see AGENTS.md), so it is
    // enabled by name rather than by turning the whole category on.
    "curly": ["error", "all"]
  },
  "env": {
    "browser": true,
    "node": true,
    "es2024": true
  },
  "ignorePatterns": [
    "**/dist/**",
    "**/build/**",
    "**/.turbo/**",
    "**/node_modules/**",
    "**/coverage/**",
    "**/.next/**",
    "**/next-env.d.ts"
  ]
}
